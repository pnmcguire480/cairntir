---
description: Semantic + metadata search across stored drawers.
---

Search Cairntir's memory using the `cairntir_recall` MCP tool. Parse the user's query, optionally scope to the current wing (project), and return the top 10 most relevant drawers.

Present results as a concise list: drawer id, room, date, and a 1-line snippet each. If the user wants full content, they'll ask.

$ARGUMENTS
