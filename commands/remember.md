---
description: Store a verbatim memory drawer in the current wing.
---

Store a new memory drawer in Cairntir. Use the `cairntir_remember` MCP tool with:

- `wing`: the current project name (infer from working directory)
- `room`: the topic (decisions, bugs, session-end, architecture, etc.)
- `content`: the verbatim text to preserve

After storing, confirm with a short acknowledgment including the drawer id.

$ARGUMENTS
